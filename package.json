{
    "name": "cards-api",
    "version": "1.0.0",
    "description": "Cloudflare workers for Cards application.",
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20230321.0",
        "typescript": "^5.0.2",
        "vitest": "^0.29.7"
    },
    "private": true,
    "scripts": {
        "start": "npx wrangler dev --local --persist",
        "start:remote": "npx wrangler dev",
        "deploy": "npx wrangler publish",
        "test": "vitest"
    }
}
